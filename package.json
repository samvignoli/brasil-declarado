{
  "name": "brasil-declarado",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 3000 --strictPort",
    "build": "vite build",
    "start": "vite preview --host 127.0.0.1 --port 3000 --strictPort",
    "test": "npm run build && node --test tests/static-site.test.mjs",
    "data": "PYTHONPATH=/tmp/receita-pydeps python3 scripts/build_deep_report.py"
  },
  "dependencies": {
    "d3-geo": "^3.1.1",
    "topojson-client": "^3.1.0"
  },
  "devDependencies": {
    "vite": "8.0.13"
  },
  "type": "module"
}
